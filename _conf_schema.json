{
  "tts_provider_id": {
    "description": "首选 TTS Provider ID（留空使用系统默认）。可用 ID 见启动日志。",
    "type": "string",
    "default": "",
    "hint": "留空 = 使用 AstrBot TTS 设置中选定的默认提供商"
  },
  "tts_fallback_provider_id": {
    "description": "兜底 TTS Provider ID（首选不可用时自动切换）。",
    "type": "string",
    "default": ""
  },
  "voice_enabled": {
    "description": "启用语音回复功能（总开关）",
    "type": "bool",
    "default": true
  },
  "sessions_whitelist": {
    "description": "允许语音的会话 ID 列表。用 /sid 获取会话 ID。留空 = 所有会话放行。",
    "type": "list",
    "items": {"type": "string"},
    "hint": "格式如 aiocqhttp:GroupMessage:123456。为空的列表代表不启用白名单。"
  },
  "sessions_blacklist": {
    "description": "禁止语音的会话 ID 列表（优先于白名单）。",
    "type": "list",
    "items": {"type": "string"}
  },
  "max_text_length": {
    "description": "TTS 文本最大长度（超过将截断）",
    "type": "int",
    "default": 500
  },
  "min_text_length": {
    "description": "TTS 文本最小长度（短于此值跳过）",
    "type": "int",
    "default": 2
  },
  "rate_limit_seconds": {
    "description": "每个会话 TTS 调用最小间隔（秒，防刷）",
    "type": "int",
    "default": 5
  },
  "density_window_minutes": {
    "description": "会话级语音密度统计窗口（分钟），超限后硬阻断",
    "type": "int",
    "default": 10
  },
  "density_max_count": {
    "description": "会话级窗口内最大语音次数，超限后完全阻止语音",
    "type": "int",
    "default": 3
  },
  "user_density_window_minutes": {
    "description": "用户级触发统计窗口（分钟），用于概率降权",
    "type": "int",
    "default": 60
  },
  "user_density_threshold": {
    "description": "用户触发频次阈值，超过后概率开始显著下降",
    "type": "int",
    "default": 5
  },
  "user_density_curve_steepness": {
    "description": "概率衰减曲线陡峭度（0=关闭用户级降权，越大越陡）",
    "type": "float",
    "default": 0.7
  },
  "voice_prompt_extra": {
    "description": "额外语音行为控制 Prompt，注入 LLM 系统提示（拟人化定制）",
    "type": "text",
    "default": "",
    "hint": "例如：用可爱的语气说话、你是个傲娇角色"
  },
  "log_level": {
    "description": "日志级别",
    "type": "string",
    "default": "info",
    "options": ["info", "debug"],
    "labels": ["INFO", "DEBUG"]
  }
}
