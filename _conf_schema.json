{
  "voice_enabled": {
    "description": "启用语音回复",
    "type": "bool",
    "default": true,
    "hint": "关闭后 AI 不会调用 ai_speak 工具"
  },

  "tts_provider_id": {
    "description": "首选 TTS 引擎",
    "type": "string",
    "_special": "select_provider_tts",
    "default": "",
    "hint": "在 AstrBot 已注册的 TTS Provider 中选择。留空则使用系统全局默认"
  },
  "tts_fallback_provider_id": {
    "description": "备用 TTS 引擎",
    "type": "string",
    "_special": "select_provider_tts",
    "default": "",
    "hint": "首选不可用时自动切换到此处。留空则不启用降级"
  },

  "min_text_length": {
    "description": "文本最短长度",
    "type": "int",
    "default": 2,
    "hint": "短于此值不合成语音，避免 AI 发送单个字/标点的语音。建议 2 ~ 10"
  },
  "max_text_length": {
    "description": "文本上限",
    "type": "int",
    "default": 500,
    "hint": "仅作参考。超长文本不会截断，会自动分段、逐段合成后再合并/发送"
  },
  "tts_segment_max_chars": {
    "description": "每段最大字符数",
    "type": "int",
    "default": 80,
    "hint": "长文本按「换行 → 句号」分割后每段最多 N 字符。建议 60 ~ 120，越小越自然但段数越多"
  },
  "tts_merge_enabled": {
    "description": "合并多段语音",
    "type": "bool",
    "default": false,
    "hint": "开启后多段音频会尝试合并为一条语音再发送。需安装 pydub / ffmpeg。注意：会增加工具耗时，可能触发 AstrBot 60s 工具超时。建议保持关闭"
  },
  "tts_merge_timeout_seconds": {
    "description": "合并超时",
    "type": "int",
    "default": 30,
    "hint": "开启合并时最多等待 N 秒，超时自动降级为分段发送。确保合成阶段不撞 60s 工具超时"
  },

  "rate_limit_seconds": {
    "description": "调用间隔",
    "type": "int",
    "default": 5,
    "hint": "同一会话两次语音至少相隔 N 秒。仅「基准限制」等级生效。0 = 不限制"
  },
  "density_window_minutes": {
    "description": "密度统计窗口",
    "type": "int",
    "default": 10,
    "hint": "在 N 分钟内统计语音数量，超限则硬阻断。仅「基准限制」等级生效。配合下方「最大次数」使用"
  },
  "density_max_count": {
    "description": "窗口内最大次数",
    "type": "int",
    "default": 3,
    "hint": "窗口内最多发送 N 条语音，超限后完全阻止直到窗口滑出。例: 10 分钟窗口内最多 3 条"
  },
  "user_density_window_minutes": {
    "description": "用户密度窗口",
    "type": "int",
    "default": 60,
    "hint": "统计单用户在 N 分钟内的语音触发次数，用于概率衰减。比会话密度窗口更长"
  },
  "user_density_threshold": {
    "description": "用户阈值",
    "type": "int",
    "default": 5,
    "hint": "超过此次数后概率开始下降（Logistic 曲线）。例: 1 小时内触发 5 次后继续触发概率逐渐降低"
  },
  "user_density_curve_steepness": {
    "description": "衰减陡峭度",
    "type": "float",
    "default": 0.7,
    "hint": "值越大衰减越急剧。0 = 关闭用户级概率降权。建议 0.3 ~ 1.5"
  },

  "default_permission_level": {
    "description": "默认权限等级",
    "type": "int",
    "default": 1,
    "options": [0, 1, 2],
    "labels": [
      "0 · 无限制 — 不检查速率和密度，随时可发语音",
      "1 · 基准限制 — 受速率间隔和双层密度控制约束",
      "2 · 完全限制 — 禁止语音（即黑名单）"
    ],
    "hint": "新会话如果没有单独配置，使用此等级。管理员私聊始终为「无限制」。可用 /voice_perm 指令或下方的「会话权限覆盖」单独配置"
  },
  "session_permissions": {
    "description": "会话权限覆盖",
    "type": "list",
    "items": {"type": "string"},
    "hint": "格式: QQ号:等级。例: 10001:0（私聊无限制）或 123456:1（群聊基准限制）。推荐用 /voice_perm 指令管理"
  },
  "sessions_blacklist": {
    "description": "旧版禁止列表",
    "type": "list",
    "items": {"type": "string"},
    "hint": "[兼容] 格式: QQ号 或 群号。已自动映射为权限等级 2。新配置请用上方「会话权限覆盖」"
  },

  "voice_prompt_extra": {
    "description": "LLM 行为注入",
    "type": "text",
    "default": "",
    "hint": "追加到系统提示词中的语音行为规则。例：「用可爱的语气说话」「每句话不超过30字」「喜欢用拟声词结尾，如 喵~」"
  },
  "backup_session_id": {
    "description": "备份转发 QQ",
    "type": "string",
    "default": "",
    "hint": "发送语音时同步将文字+音频文件转发到指定的 QQ 群或好友。格式：群号 或 QQ号:friend。例: 123456（转发到群 123456）或 10001:friend（转发到好友 10001）。留空不转发"
  },

  "local_audio_dir": {
    "description": "本地音频存档目录",
    "type": "string",
    "default": "",
    "hint": "TTS 生成的音频文件会复制一份到该目录持久保留。留空则使用默认路径 data/plugin_data/astrbot_plugin_voice_assistant/tts_archive"
  },
  "local_audio_retention_days": {
    "description": "本地存档保留天数",
    "type": "int",
    "default": 7,
    "hint": "超过 N 天的音频文件会被自动清理。设为 0 则永不清除"
  },

  "cloud_backup_enabled": {
    "description": "启用云存储备份",
    "type": "bool",
    "default": false,
    "hint": "开启后每次生成语音时，将音频文件上传到所选后端"
  },
  "cloud_backend": {
    "description": "云存储后端类型",
    "type": "string",
    "default": "custom",
    "options": ["custom", "s3", "webdav", "smb"],
    "labels": ["自定义 API", "S3 兼容", "WebDAV", "SMB 共享"]
  },
  "cloud_custom_url": {
    "description": "自定义·请求地址",
    "type": "string",
    "default": "",
    "hint": "[custom] 上传接口 URL。文件将以 multipart/form-data 格式发送，文件字段名为 file"
  },
  "cloud_custom_headers": {
    "description": "自定义·请求头",
    "type": "text",
    "default": "",
    "hint": "[custom] 每行一条，格式: Key: Value。例:\nAuthorization: Bearer xxx\nX-Custom: value"
  },
  "cloud_custom_body": {
    "description": "自定义·附加请求体",
    "type": "text",
    "default": "",
    "hint": "[custom] JSON 对象，与文件一起以 multipart/form-data 发送。例: {\"album\": \"tts\"}"
  },
  "cloud_custom_result_path": {
    "description": "自定义·返回 URL 路径",
    "type": "string",
    "default": "",
    "hint": "[custom] 从上传返回 JSON 中提取最终 URL 的路径，支持点语法。例: data.url"
  },
  "cloud_s3_endpoint": {
    "description": "S3·端点地址",
    "type": "string",
    "default": "",
    "hint": "[s3] S3 兼容存储的端点 URL。例: https://s3.amazonaws.com 或 https://play.min.io"
  },
  "cloud_s3_region": {
    "description": "S3·区域",
    "type": "string",
    "default": "",
    "hint": "[s3] 存储区域。例: us-east-1, ap-northeast-1"
  },
  "cloud_s3_bucket": {
    "description": "S3·Bucket",
    "type": "string",
    "default": "",
    "hint": "[s3] 存储桶名称"
  },
  "cloud_s3_access_key": {
    "description": "S3·Access Key",
    "type": "string",
    "default": "",
    "hint": "[s3] 访问密钥 ID"
  },
  "cloud_s3_secret_key": {
    "description": "S3·Secret Key",
    "type": "string",
    "default": "",
    "hint": "[s3] 访问密钥 Secret"
  },
  "cloud_s3_path_style": {
    "description": "S3·Path Style",
    "type": "bool",
    "default": true,
    "hint": "[s3] true=路径风格 (/bucket/key)，false=虚拟主机风格 (bucket.endpoint/key)"
  },
  "cloud_webdav_url": {
    "description": "WebDAV·端点地址",
    "type": "string",
    "default": "",
    "hint": "[webdav] WebDAV 服务器目录 URL。例: https://nextcloud.example.com/remote.php/dav/files/user/"
  },
  "cloud_webdav_username": {
    "description": "WebDAV·用户名",
    "type": "string",
    "default": "",
    "hint": "[webdav] 基本认证用户名"
  },
  "cloud_webdav_password": {
    "description": "WebDAV·密码",
    "type": "string",
    "default": "",
    "hint": "[webdav] 基本认证密码"
  },
  "cloud_smb_share": {
    "description": "SMB·共享路径",
    "type": "string",
    "default": "",
    "hint": "[smb] 共享目录路径。例: \\\\server\\share\\backup 或 //server/share/backup"
  },
  "cloud_smb_username": {
    "description": "SMB·用户名",
    "type": "string",
    "default": "",
    "hint": "[smb] 访问共享的用户名"
  },
  "cloud_smb_password": {
    "description": "SMB·密码",
    "type": "string",
    "default": "",
    "hint": "[smb] 访问共享的密码"
  },
  "cloud_smb_domain": {
    "description": "SMB·域",
    "type": "string",
    "default": "",
    "hint": "[smb] 域（可选）。Windows 网络环境可能需要填写"
  },

  "log_level": {
    "description": "日志级别",
    "type": "string",
    "default": "info",
    "options": ["info", "debug"],
    "labels": [
      "INFO — 仅记录关键操作（调用、权限、合成、发送）",
      "DEBUG — 输出所有内部调试信息"
    ],
    "hint": "工具调用和密度判定始终输出 info 日志"
  }
}
